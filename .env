# 监听地址: ip:port
LISTEN_ADDR=0.0.0.0:8080

# 32字节密钥的 base64url(no padding) 编码，必填
# 可通过: openssl rand -base64 32 | tr '+/' '-_' | tr -d '='
DECRYPT_KEY_B64=mzO9eyHq0BFYBmlO5Vqb-3z0HKN_VDaNZ_bKodCKxbo

# URL 加密参数名（query）
URL_PARAM_NAME=u

# 透传请求超时（秒）
REQUEST_TIMEOUT_SECS=30
